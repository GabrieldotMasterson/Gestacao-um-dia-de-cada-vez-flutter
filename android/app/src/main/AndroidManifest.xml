<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestacao">
    <application
        android:label="umdiadecadavez"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Home Screen Widget -->
        <receiver
            android:name=".DailyMessageWidgetProvider"
            android:label="Mensagem do Dia"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>
        <!-- Widget Padrão -->
        <receiver
            android:name=".DailyMessageWidgetPadraoProvider"
            android:exported="true"
            android:label="Mensagem Padrão">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_padrao_info" />
        </receiver>

        <!-- Widget Floral -->
        <receiver
            android:name=".DailyMessageWidgetFloralProvider"
            android:exported="true"
            android:label="Mensagem Floral">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_floral_info" />
        </receiver>

        <!-- Widget Oceano -->
        <receiver
            android:name=".DailyMessageWidgetOceanoProvider"
            android:exported="true"
            android:label="Mensagem Oceano">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_oceano_info" />
        </receiver>

        <!-- Widget Transformação -->
        <receiver
            android:name=".DailyMessageWidgetTransformacaoProvider"
            android:exported="true"
            android:label="Mensagem Transformação">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android:appwidget.provider"
                android:resource="@xml/widget_transformacao_info" />
        </receiver>

        <!-- Widget Noite -->
        <receiver
            android:name=".DailyMessageWidgetNoiteProvider"
            android:exported="true"
            android:label="Mensagem Noite">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_noite_info" />
        </receiver>

        <!-- Widget Natureza -->
        <receiver
            android:name=".DailyMessageWidgetNaturezaProvider"
            android:exported="true"
            android:label="Mensagem Natureza">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_natureza_info" />
        </receiver>

        <!-- Widget Seu Corpo -->
        <receiver
            android:name=".DailyMessageWidgetSeuCorpoProvider"
            android:exported="true"
            android:label="Mensagem Seu Corpo">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_seucorpo_info" />
        </receiver>

        <!-- Widget Sol -->
        <receiver
            android:name=".DailyMessageWidgetSolProvider"
            android:exported="true"
            android:label="Mensagem Sol">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_sol_info" />
        </receiver>

        <!-- Widget Serenidade -->
        <receiver
            android:name=".DailyMessageWidgetSerenidadeProvider"
            android:exported="true"
            android:label="Mensagem Serenidade">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_serenidade_info" />
        </receiver>

        <!-- Widget Força -->
        <receiver
            android:name=".DailyMessageWidgetForcaProvider"
            android:exported="true"
            android:label="Mensagem Força">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_forca_info" />
        </receiver>

        <!-- Widget Ternura -->
        <receiver
            android:name=".DailyMessageWidgetTernuraProvider"
            android:exported="true"
            android:label="Mensagem Ternura">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_ternura_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
